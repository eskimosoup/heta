<% title CourseCategory.model_name.human.pluralize %>

<div class="admin_functions">
  <%= link_to "New #{ CourseCategory.model_name.human }", new_admin_course_category_path %>
</div>

<%= informant_table @course_categories, {
      :headings => [ 'Title', 'Colour', '', '' ], 
      :body => [ :title, :colour, it_edit, it_destroy ]
    } %>
